services:
  - type: web
    name: timeshift-radio
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m uvicorn app:app --host 0.0.0.0 --port $PORT --loop asyncio --http h11
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
